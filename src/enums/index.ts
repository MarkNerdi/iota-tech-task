export enum FilterCategories {
    NAME = "Name",
    ADDRESS = "Address",
    BALANCE_BIGGER = "Balance bigger than",
    BALANCE_SMALLER = "Balance smaller than"
}

export enum OrderCategories {
    NAME = "name",
    ADDRESS = "address",
    BALANCE = "balance"
}
export enum Orders {
    NONE = 0,
    DESC = 1,
    ASC = 2,
}